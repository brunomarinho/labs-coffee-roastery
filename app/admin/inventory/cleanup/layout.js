export const metadata = {
  title: 'Limpeza de Reservas - Admin',
  robots: {
    index: false,
    follow: false,
    nocache: true,
    nosnippet: true,
    noimageindex: true
  }
}

export default function CleanupLayout({ children }) {
  return children
}